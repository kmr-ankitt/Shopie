import Button from "../components/Button";
import Nav from "../components/Nav";

function FirstPage() {
  return (
    <div>
      <Nav />
      <h1>
        Let's Explore Let's uncover your <span>Ideal</span> Items
      </h1>
      <p>List is full of possiblities, as is you wishlist with unique items!</p>
      <Button name="Buy Now" link="#" />
    </div>
  );
}

export default FirstPage;
